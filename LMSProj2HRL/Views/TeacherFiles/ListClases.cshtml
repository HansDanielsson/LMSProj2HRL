@model IEnumerable<string>
@{
    ViewBag.Title = "ListClases";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string path = "~/FileHandler/";
}

<h2>Inlämningar per Klass</h2>

<ul>
    @if (Model != null)
    {
        foreach (var fName in Model)
        {
            var name = fName;
            var link = @Url.Content(path) + name.Replace(" ", "%20");

            <li>
                <a href="@link">@name</a>
            </li>
        }
    }
</ul>
