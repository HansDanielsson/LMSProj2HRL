@model IEnumerable<string>
@{
    ViewBag.Title = "Inlämningar-Klass";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string path = "~/TeacherFiles/ListTeacherFiles?id=";
}

<h2>Inlämningar per Klass</h2>

<ul>
    @if (Model != null)
    {
        foreach (var fName in Model)
        {
            var name = fName;
            var link = @Url.Content(path) + name.Replace(" ", "%20");

            <li>
                <a href="@link">@name</a>
            </li>
        }
    }
</ul>
