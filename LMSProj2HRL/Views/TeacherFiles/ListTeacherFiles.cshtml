@model IEnumerable<string>
@{
    ViewBag.Title = "Lista Filer";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lista Filer</h2>

<ul>
    @if (Model != null)
    {
        foreach (var fName in Model)
        {
            var name = fName;
            var link = @Url.Content("~/FileHandler/") + name.Replace(" ", "%20");

            <li>
                <a href="@link">@name</a>
            </li>
        }
    }
</ul>