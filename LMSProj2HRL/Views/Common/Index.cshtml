@model IEnumerable<string>
@{
    ViewBag.Title = "Gemensam";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Gemensam</h2>

<ul>
    @if (Model != null)
    {
        foreach (var fName in Model)
        {
            var name = fName;
            var link = @Url.Content("~/FileHandler/Shared/") + name.Replace(" ", "%20");

            <li>
                <a href="@link">@name</a>
            </li>
        }
    }
</ul>

<div>
    @using (Html.BeginForm("Index", "Common", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <input type="file" name="UpFile" id="UpFile" value="Välj fil" />
        <button type="submit">Ladda up</button>
        <br />
        <br />
        @ViewBag.Message
    }
</div>